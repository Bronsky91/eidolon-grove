// TODO: Get from db/server?
const initialState = {
  allSproutlings: ["test1", "test2"],
  mainSproutling: "test2",
};

const sproutlingsReducer = (state = initialState, action) => {
  return state;
};

export default sproutlingsReducer;
